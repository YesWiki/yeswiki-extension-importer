parameters:
  sync_secret: ""
  importer_editable_config_params:
    - "sync_secret"

services:
  _defaults:
    autowire: true
    public: true

  YesWiki\Importer\Service\:
    resource: "services/*"
    autowire: false

  YesWiki\Importer\Service\ImporterManager:
    autowire: true

  # Allows to use controllers as services
  YesWiki\Importer\Controller\:
    resource: "controllers/*"
